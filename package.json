{
  "name": "search-string",
  "version": "3.1.0",
  "description": "Another parser for advanced search query syntax.",
  "main": "dist/node/index.js",
  "types": "dist/node/index.d.ts",
  "files": [
    "index.ts",
    "src",
    "dist"
  ],
  "scripts": {
    "tsBuild": "tsc",
    "tsWatch": "tsc --watch",
    "build": "rm -rf dist/node && npm run tsBuild",
    "ci": "npm run lint && npm run test && npm run build",
    "lint": "eslint .",
    "prepare": "npm run ci",
    "prepublishOnly": "npm run ci",
    "test": "jest",
    "testWatch": "jest --watch"
  },
  "author": "Marcus Ericsson <marcus@mixmax.com> (https://mixmax.com)",
  "license": "MIT",
  "keywords": [
    "query parser",
    "querystrings",
    "search",
    "search syntax parser",
    "search query"
  ],
  "repository": "git@github.com:mixmaxhq/search-string.git",
  "devDependencies": {
    "eslint": "^4.7.2",
    "eslint-config-mixmax": "^0.11.0",
    "jest": "^24.8.0",
    "jest-cli": "^24.8.0",
    "@types/jest": "^24.0.15",
    "typescript": "^3.5.3",
    "ts-jest": "^24.0.2"
  },
  "dependencies": {}
}
